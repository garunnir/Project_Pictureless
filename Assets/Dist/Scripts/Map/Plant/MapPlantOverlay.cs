// ============================================================
// MapPlantOverlay — deprecated; plant/till live on TileData (OccupiedCell / floor)
// ============================================================

namespace IsoTilemap
{
    // Intentionally empty. Legacy type kept so .meta GUID stays stable if referenced.
    // Runtime SSOT: MapPlantHost + TileMapModel.
}
